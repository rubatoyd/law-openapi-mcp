# law-openapi-mcp — 프로젝트 지침

> **법제처 국가법령정보(law.go.kr DRF)** 수집기. 공개 **MCP 서버 + CLI**.
> 자매 프로젝트 **na-openapi-mcp**(`../NA openAPI`) · **nl-openapi-mcp**(`../NL openAPI`) ·
> **kci-openapi-mcp**(`../KCI openAPI`) · **scienceON-mcp**(`../scienceon`) 와 동일 아키텍처.
> API 규격 → [docs/LAW_API_GUIDE.md](docs/LAW_API_GUIDE.md) · 이력 → [docs/작업일지.md](docs/작업일지.md)

## 1. 목표
연구 자료수집 단계에서 **법령·판례·정책 문헌**을 검색·수집해 후속 분석과 **인용**의
입력을 안정적으로 생산한다. 자매 3종과 상호보완: KCI·ScienceON = 학술논문,
NL = 단행본·회색문헌, NA = 입법·정책 문헌(국회), **LAW = 법령·판례(법제처)**.

발단은 Ibis(서지·인용 관리 앱)의 과제 T41 이다 — 자세한 배경은 그쪽
`docs/TASKS.md` T41. Ibis 는 이 MCP 를 레일 넷째 화면으로 받는다.

## 2. 🔴 수집 단위는 인용 단위다 (사용자 지시 — 바꾸지 말 것)

검색·수집 단위는 **인용 관행**을 따른다. 서지 메타데이터에 맞아야 하고 학술 문헌에
인용 가능한 형태여야 하기 때문이다.

> **법령 하나가 문헌 하나**다. 조문은 인용할 때의 **위치**(쪽 자리)로 넣는다.
> 판례는 사건 하나, 법령해석례는 안건 하나, 조약은 조약 하나.

⛔ **조문 단위로 쪼개 색인하지 않는다.** 「초·중등교육법」은 조문 135개짜리 문서지만
문헌으로는 하나다. 조문마다 레코드를 만들면 서지가 아니라 말뭉치가 되고, 참고문헌
목록에 「초·중등교육법 제23조」가 135줄 생긴다. 본문이 필요하면 `law_body` 로 받는다.

## 3. 확정 결정사항
| 항목 | 결정 |
|------|------|
| 언어/런타임 | Python 3.10+ |
| 패키지 관리 | **uv**. venv 는 **클라우드 폴더 밖** `C:/Users/rubat/.venvs/law-openapi-mcp` (`UV_PROJECT_ENVIRONMENT`, `.claude/settings.local.json`) |
| 의존성 | mcp(FastMCP, **`<2` 상한 필수**), requests, openpyxl, python-dotenv, truststore |
| 인터페이스 | 공용 코어 + **MCP 서버(server.py)** + **CLI(cli.py)** |
| 대상 API | **법제처 DRF** `www.law.go.kr/DRF/{lawSearch,lawService}.do` (XML) |
| 출력 | xlsx · csv · json · sqlite |
| 공개 | MIT. `.env`·`.claude/settings.local.json`·`output/`·`probe-out/` 는 gitignore |

⛔ **data.go.kr 공유서비스(1170000)를 쓰지 않는다** — 목록 8종뿐이고 본문도 판례도
없다. 자세한 대조는 GUIDE §0.

## 4. 구조 (자매 프로젝트와 레이어 일치)
```
src/law_mcp/
  config.py     # .env, 엔드포인트, ★ TARGETS 레지스트리(갈래별 실측 태그), 검증
  models.py     # Record 스키마 + 정규화(날짜·HTML) + 인용 필드 매핑
  parser.py     # 목록·본문 파싱 · HTTP 200 실패 봉투 4종 판별 · 조문/항/호 렌더
  client.py     # 페이징·재시도·절단 메타 + 축 실패에도 계속하는 수집
  exporters.py  # xlsx/csv/json/sqlite (+ 미매핑 필드 열 승격, safe_name)
  server.py     # MCP 도구 6종
  cli.py        # status / targets / search / body / collect
docs/           # LAW_API_GUIDE.md(★ 실측 규격) · 작업일지.md
scripts/probe_api.py   # ★ 라이브 탐침 — 봉투·상한·실패·HTML전용 재측정
tests/fixtures/*.xml   # ★ 2026-09-08 의 진짜 응답(OC 만 가림)
tests/          # 실응답 기반 회귀
```

## 5. 자격증명
- 변수: **`LAW_OC`**. open.law.go.kr 신청 시 지정하는 **가입 이메일의 @ 앞부분**.
- ⚠️ **비밀값이 아니다.** URL 에 평문으로 나가고 응답의 `…상세링크` 에도 되돌아온다.
  그래서 `scrub()` 대상이 아니다 — 지우면 링크가 깨진다. 다만 개인 식별자이므로
  공개 픽스처에서는 `__OC__` 로 가린다.
- `LAW_OS_TRUST`(기본 1): 교육망·사내망 SSL 인터셉션 대응.

## 6. 핵심 기술사실 (2026-09-08 라이브 실측)
> 전체 근거표 → [docs/LAW_API_GUIDE.md](docs/LAW_API_GUIDE.md). 재현 → `scripts/probe_api.py`

### (A) 🔴 갈래마다 태그가 다르고 이름에서 유추할 수 없다 — **예외가 아니라 규칙이다**
전수 실측 62종 중 **45종이 제 이름이 아닌 레코드 태그**를 쓴다: 부처 해석 39종 전부
`<cgmExpc>`, 특별행정심판 4종 `<decc>`, `school`→`<admrul>`, `admbyl`→`<admrulbyl>`,
`ordin`·`lsStmd`→`<law>`. 본문 루트는 20가지고, 식별자 파라미터는 `ID`/`MST` **말고도**
있다(`lstrm` = `trmSeqs`). → `config.TARGETS` 는 손으로 적은 표가 아니라 **측정 결과를
생성한 것**이다(`scripts/probe_catalog.py`). 규칙으로 바꾸지 말 것.
⚠️ **`totalCnt=0` 을 '무효 target' 으로 읽지 말 것** — 검색어가 안 맞았을 뿐이다.
   1차 측정에서 18종을 그렇게 오판할 뻔했고, 질의어를 바꾸니 전부 정상이었다.

### (B) 🔴 모든 실패가 HTTP 200 이다
없는 target = **0바이트** · 잘못된 OC = `<Response>` · 없는 식별자 = `<Law>…없습니다</Law>`
(**target 과 무관하게 루트가 `Law`**). 상태코드로 판단하는 코드를 쓰지 말 것.

### (C) 🔴 `query` 를 빼면 전체 카탈로그가 온다
오류가 아니다(현행법령 5,614건). `validate_query()` 가 호출 전에 막는다.

### (D) 🔴 조문의 알맹이는 `항`·`호` 안에 있다
`조문내용` 만 읽으면 본문의 **70%를 잃는다**(실측 2,057자 / 7,359자). 조용히 줄어든다.
`조문여부='전문'` 은 장·절 표제이지 조문이 아니다.

### (E) 🔴 판례 본문은 데이터출처에 따라 없다
`데이터출처명=대법원` 은 오고 `국세법령정보시스템` 은 안 온다. **결손이지 오류가 아니다** —
`NotFound` 로 구분해 올리고, 수집기는 기록하고 넘어간다.

### (F) 상한 — 자매 API 의 사실을 이식하지 말 것
`display` 500 · **`page` 하드 상한 없음**(totalCnt 전량 회수 가능).
국회도서관의 `pageno<=99`(회수 한계 99,000건)와 **다르다**.
⚠️ `numOfRows` 는 요청 크기가 아니라 **실제 반환 건수**다 — `에코 != 요청` 만 보면
결과가 적을 때마다 거짓 경고가 나고 페이지 크기가 줄어든다(초판이 그랬다).

### (G) 🔴 텍스트의 꺾쇠는 HTML 이 아닐 수 있다
`<개정 2010.7.6>`·`부칙 <2010.7.5>` 는 **내용**이다. 임의의 `<...>` 를 지우면 사라진다.
알려진 HTML 태그 이름만 지운다(`models.clean_text`).

### (H) 같은 필드가 목록과 본문에서 다른 날짜 꼴로 온다
판례 `선고일자`: 목록 `2026.04.23` / 본문 `20260212`. 둘 다 받는다.

## 7. 개발 원칙 (자매 프로젝트 공통 — 이미 값을 치른 것들)
- 자격증명은 `.env`/MCP env 로만. **`raise_for_status()` 금지** — URL 을 예외에 박는다.
- **조용한 절단 금지** — `total`·`truncated`·경고를 메타로 노출. 빈 응답을 '0건'으로
  통과시키지 않는다.
- 🔴 **미매핑 필드를 버리지 않는다.** 정규화 표에 없는 원본 필드도 csv/xlsx 에 **열로
  승격**하고 json/sqlite 에는 `raw` 를 싣는다. 자매 저장소는 이게 없어서 국회의안의
  '제안이유 및 주요내용'(97% 채워진 서술형 본문)을 통째로 잃었다.
- 🔴 **한 축이 실패해도 앞서 모은 것을 버리지 않는다.** 갈래·검색어마다 결과를 보존하고
  실패는 `failed_axes` 로 보고한다. 자매 저장소는 첫 페이지 실패를 그대로 올려 수집
  전체를 잃었다.
- 설정 오류(OC 누락 등)는 **재시도 루프에 들여보내지 않는다** — 영구 조건이 '결과 0건'
  처럼 보이면 안 된다.
- 출력 경로 이탈 차단(`safe_name`). 내보내기는 **쓰기 전에 형식을 전부 검증**한다.
- MCP 경계에서는 조용하게(`@_safe` 가 예외를 dict 로), 파서 내부는 시끄럽게.
- 전송 기본값은 **stdio 로 못박는다** — 바뀌면 기존 등록이 전부 죽는다(회귀로 고정).
- `__version__` 은 `importlib.metadata` 조회. `use_os_trust()` 는 **코드**에서 호출.
- 정중한 호출: throttle, 지수 백오프, 페이지네이션 안전장치(새 레코드 0이면 종료).
- **라이브 검증 우선(추정 금지)** — 필드명·값은 실응답을 떠서 확정하고 검증 등급을 표기한다.
  ⚠️ **자매 저장소의 사실을 이식하지 말 것** — 이 API 는 charset 을 붙여 주고, page
  상한이 없고, 마크업이 섞이는 자리가 다르다. 같은 회사가 만든 API 도 다르다.
- **커밋 메시지 한국어, Claude 서명 금지.**

## 8. 상태 (2026-09-08)
- ✅ **v0.1.0 코어 완성** — config·models·parser·client·exporters·server·cli +
  회귀 **77건 통과**. 라이브 검증 완료: `law status` 왕복, `search`·`body`·`collect`
  (3갈래×2검색어 40건 → xlsx/csv/json, 호출 6회), MCP stdio 핸드셰이크 + 도구 6종 등록.
- 🔬 **실측이 T41 의 두 미해결을 정리했다**: `lsHistory`·`couseLs` 는 미해결이 아니라
  **XML 미지원이 사실**이다(응답으로 확인). 남은 미확인은 **부처 1차 법령해석 target 코드**
  하나이고, 안내 페이지가 JS 렌더링이라 브라우저로 봐야 한다(GUIDE §11).
- 🔴 **자체 결함 3종을 테스트·라이브가 잡았다** — 전부 내가 그날 넣은 것이다:
  ① `조문내용` 만 읽어 **본문의 70%를 잃었다**(항·호 누락). 오류는 안 났다.
  ② 로그 scrubber 가 모든 인자를 `str()` 로 바꿔 `%d` 포맷을 터뜨렸다 — 재시도 경고
     경로가 실제로 숫자 인자를 쓴다.
  ③ `numOfRows` 를 '요청한 페이지 크기'로 오해해, 결과가 적을 때마다 거짓 경고를 내고
     페이지 크기를 3으로 줄였다(라이브 첫 실행에서 드러났다).
  → 셋 다 회귀로 고정했다. **①·③ 은 단위 테스트만으로는 안 보였다** — 실응답 픽스처와
     라이브 실행이 각각 잡았다.
- 🔬 **탐침이 내 레지스트리 오류 3종을 잡았다** — `eflaw`·`elaw`·`lsStmd` 가 `law` 계열과
  같으리라 짐작했는데 셋 다 틀렸다(레코드 태그 `law` · 본문 루트 `Law` · `efYd` 필수).
  **짐작한 자리가 정확히 틀린 자리였다.**

### 2026-09-08 2차 — 갈래 10 → **72종**, CI 이식
- ✅ **T41 의 마지막 미해결을 풀었다** — 부처 1차 법령해석 target 은 **`<부처>CgmExpc`**
  (`moeCgmExpc`)로 **순서가 뒤집혀** 있다. 안내 함수명은 `cgmExpcMoeListGuide` 인데
  target 은 반대다. 그래서 부처 코드 단독 후보 9개가 전부 빈 응답이었다.
  안내 페이지가 JS 렌더링이라 **브라우저로** 열어 195건에서 target 99종을 뽑았다
  → [docs/DRF_CATALOG.md](docs/DRF_CATALOG.md).
- ✅ 후보 62종 전수 실측(`scripts/probe_catalog.py`) → 레지스트리 **72종**. 회귀 **84건**.
  라이브 확인: `moeCgmExpc` 81건 · `decc` 60 · `lstrm` 823 · `licbyl` 1,085 · `ppc` 41 ·
  `ttSpecialDecc` 432.
- 🔴 **탐침 자체의 결함 셋을 잡았다** — 측정 도구도 측정 대상이다:
  ① `totalCnt=0` 을 '응답 없음'으로 적을 뻔했다(18종). 질의어를 바꾸니 전부 정상.
  ② 결과가 1건일 때 '가장 많이 반복되는 자식' 규칙이 봉투 필드 `기관명` 을 레코드로 골랐다.
  ③ 식별자 파라미터를 `MST`/`ID` 로만 찾아 `lstrm` 의 `trmSeqs` 를 놓쳤다.
     → 상세링크에 파라미터 이름이 실려 있으므로 **거기서 읽게** 고쳤다(짐작 제거).
- ✅ CI 이식(`.github/workflows/ci.yml`) — 테스트·비밀 미추적·픽스처 OC 가림·mcp 1.x 신규
  해석·콜드스타트 핸드셰이크·무인증 CLI. ⚠️ `astral-sh/setup-uv` 는 이동 태그가 없어 정확 고정.
- ⏭️ 다음: ① 릴리스 파이프라인(지시 대기 — 공개 여부가 정해져야 한다) ② Ibis 연동
  ③ 위원회·특별행정심판 본문 렌더 점검(현재는 일반 경로로만 훑었다)
