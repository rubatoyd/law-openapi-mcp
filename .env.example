# 법제처 OPEN API 는 `OC` 로 인증한다 — **비밀값이 아니다.**
# open.law.go.kr 에 가입할 때 쓴 이메일의 @ 앞부분을 사용자가 직접 지정한다.
# URL 에 평문으로 실려 나가고, 응답의 상세링크에도 그대로 되돌아온다.
# 그래도 .env 에 두는 이유는 사람마다 값이 다르기 때문이지 숨기려는 것이 아니다.
LAW_OC=your_id_here

# 교육망·사내망의 SSL 인터셉션 대응(OS 신뢰저장소 사용, 검증은 유지). 0 이면 비활성.
LAW_OS_TRUST=1
